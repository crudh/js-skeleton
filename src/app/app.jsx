/**
 * @jsx React.DOM
 */
define(["lodash", "react"], function(_, React) {
    var AppContainer = React.createClass({
       render: function() {
           return <h1>REACT IS LIVE!</h1>;
       }
    });

    return AppContainer;
});