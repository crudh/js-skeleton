/**
 * @jsx React.DOM
 */
define(["underscore", "react"], function(_, React) {
    var AppContainer = React.createClass({
        render: function() {
            return <div>
                <h1>REACT IS LIVE!</h1>
            </div>;
        }
    });

    return AppContainer;
});